#include <bits/stdc++.h>
using namespace std;

int main() {
    int y = 6;
    int x = 3;

    cout << (y += x) << endl; 
    cout << (y -= x) << endl; 
    cout << (y *= x) << endl; 
    cout << (y /= x) << endl; 
    cout << (y %= x) << endl; 
    
    return 0;
}