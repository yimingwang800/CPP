#include <bits/stdc++.h>
using namespace std;

int main() {
    int x = 0;
    while (x < 5) {
        cout << "x is smaller than 10" << endl;
        cout << x << endl;

        x++;
    }
    
    return 0;
}